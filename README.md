# Wfetch 🚀

**Минималистичный системный fetch с красивыми цветами**

![Version](https://img.shields.io/badge/version-3.5.0-blue)
![Python](https://img.shields.io/badge/python-3.6+-green)
![Platform](https://img.shields.io/badge/platform-Windows%20%7C%20Linux%20%7C%20macOS-lightgrey)
![License](https://img.shields.io/badge/license-MIT-orange)

Быстрый и красивый инструмент для отображения системной информации в терминале с автоматической сменой цветовых схем.

## ✨ Особенности

- 🎨 **Новые цвета каждый день** — 5 гармоничных схем
- 📊 **Живая статистика памяти** — с индикатором прогресса
- 🖼️ **Два логотипа** — W (85%) и альтернативный (15%)
- ⚡ **Мгновенный запуск** — <100ms
- ⚙️ **JSON конфиг** — полная кастомизация
- 🐧 **Кроссплатформенность** — Windows, Linux, macOS

## 📸 Демо

![Wfetch Demo](demo.png)

```bash
██╗    ██╗  OS       Windows 11 Pro 23H2
██║    ██║  Host     DESKTOP-PC123
██║ █╗ ██║  Kernel   10.0.22621.3007
██║███╗██║  Uptime   5h 30m
╚███╔███╔╝  Packages 156
 ╚══╝╚══╝   Memory   8.4/32.0 GB [███░░░░░░░] 27%

Colors:  ████████████
```

## 🚀 Быстрая установка

```bash
# Установите зависимости
pip install psutil

# Установите wfetch
pip install .

# Создайте конфиг
wfetch --gen-config

# Запустите!
wfetch
```

## ⚙️ Базовое использование

```bash
# Основной вывод
wfetch

# Компактный режим
wfetch --compact

# Без цветов
wfetch --no-colors

# Показать конфиг
wfetch --config

# Создать новый конфиг
wfetch --gen-config

# Помощь
wfetch --help
```

## 📁 Конфигурация

### 📍 Расположение конфига
| Система | Путь |
|---------|------|
| **Windows** | `%APPDATA%\wfetch\config.json` |
| **Linux/macOS** | `~/.config/wfetch/config.json` |



**4. Изменить цвета (ручной режим):**
```json
{
  "colors": {
    "primary": 213,    // Розовый (0-255)
    "secondary": 117,  // Голубой
    "accent": 141,     // Фиолетовый
    "text": 250,       // Серый текст
    "highlight": 84    // Зеленый акцент
  },
  "behavior": {
    "random_colors": false  // Отключить случайные цвета
  }
}
```

### 🎨 Готовые цветовые схемы

Добавьте в конфиг:

```json
{
  "colors": {
    "scheme": "nord",  // или "dracula", "gruvbox", "monokai"
    "random_colors": false
  }
}
```

## 🐛 Решение проблем

### 🔧 Конфиг не применяется
```bash
# 1. Удалите старый конфиг
# Windows:
del %APPDATA%\wfetch\config.json
# Linux/macOS:
rm ~/.config/wfetch/config.json

# 2. Создайте новый
wfetch --gen-config

# 3. Отредактируйте и сохраните
# 4. Перезапустите
wfetch
```

### 📦 Пакеты показывают "?" на Windows
По умолчанию отключено (медленно). Чтобы включить:
```json
{
  "behavior": {
    "windows_packages": true
  }
}
```

### 🖥️ Нет цветов в терминале
```bash
# Используйте терминал с поддержкой ANSI:
# - Windows Terminal (рекомендуется)
# - Windows: ConEmu, mintty
# - Linux/macOS: стандартный терминал

# Или запустите без цветов:
wfetch --no-colors
```

### 🔄 Переустановка
```bash
# Полная переустановка:
pip uninstall wfetch
pip install .
wfetch --gen-config
```

## 📊 Что отображается

### На всех системах:
- **OS** — Windows 11 Pro / Ubuntu 22.04 / macOS Ventura
- **Host** — имя компьютера (DESKTOP-ABC123)
- **Kernel** — версия ядра/сборки
- **Uptime** — время работы системы
- **Memory** — использование ОЗУ с индикатором
- **Shell** — bash/zsh/fish/PowerShell/cmd

### Только Linux:
- **Packages** — количество пакетов (pacman/apt/dnf)
- **WM/DE** — окружение рабочего стола (KDE/GNOME/XFCE)

### Только Windows (опционально):
- **Packages** — количество установленных программ

## 🎯 Примеры конфигов

### Минималистичный
```json
{
  "display": {"compact": true, "spacing": 1},
  "info": {"os": true, "host": true, "memory": true},
  "behavior": {"show_color_bar": false}
}
```

### Для серверов
```json
{
  "info": {"os": true, "host": true, "kernel": true, 
           "uptime": true, "memory": true},
  "behavior": {"random_colors": false, "alt_logo_chance": 0}
}
```

### Максимальный
```json
{
  "display": {"border": true, "spacing": 3},
  "info": {"os": true, "host": true, "kernel": true, "uptime": true,
           "packages": true, "memory": true, "shell": true, "wm_de": true},
  "behavior": {"alt_logo_chance": 50, "gradient_logo": true}
}
```

## 🔗 Интеграция

### Автозапуск при открытии терминала

**Bash/Zsh** (`~/.bashrc` или `~/.zshrc`):
```bash
# Добавьте в конец файла
if command -v wfetch &> /dev/null; then
    wfetch
fi
```

**PowerShell** (`$PROFILE`):
```powershell
# Создайте профиль если нет
if (!(Test-Path -Path $PROFILE)) {
    New-Item -ItemType File -Path $PROFILE -Force
}

# Добавьте в профиль
if (Get-Command wfetch -ErrorAction SilentlyContinue) {
    wfetch
}
```

**Fish** (`~/.config/fish/config.fish`):
```bash
if command -v wfetch > /dev/null
    wfetch
end
```

### Полезные алиасы
```bash
# Linux/macOS (~/.bashrc)
alias wf='wfetch'
alias wfc='wfetch --compact'
alias wf-config='nano ~/.config/wfetch/config.json'

# Windows PowerShell ($PROFILE)
function wf { wfetch }
function wfc { wfetch --compact }
function wf-config { code $env:APPDATA\wfetch\config.json }
```

## 🤝 Участие

Нашли баг или есть идея? [Создайте issue](https://github.com/Minish777/wfetch/issues)!

1. Форкните репозиторий
2. Создайте ветку для фичи
3. Зафиксируйте изменения
4. Отправьте Pull Request

## 📞 Поддержка

- **Issues**: [GitHub Issues](https://github.com/Minish777/wfetch/issues)
- **Переустановка**: `pip uninstall wfetch && pip install .`
- **Прямой запуск**: `python wfetch.py` (для тестирования)

---

<p align="center">
  Сделано с ❤️ для терминальных энтузиастов
  <br>
  <sub>Если нравится — поставьте звезду на GitHub!</sub>
</p>

## 🎯 Краткий чек-лист

```bash
# Установка (30 секунд)
pip install psutil
pip install .
wfetch --gen-config
wfetch

# Настройка
1. Откройте конфиг (см. путь выше)
2. Внесите изменения
3. Сохраните файл
4. Запустите wfetch

# Проблемы?
1. Удалите конфиг
2. wfetch --gen-config  
3. wfetch
```

**Готово! Теперь у вас красивый системный fetch в терминале.** 🎉
